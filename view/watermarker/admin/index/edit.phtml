<?php
/**
 * Template file for watermarker edit action
 */
$this->htmlElement('body')->appendAttribute('class', 'watermarker edit');
$form = $this->form;
?>

<?php echo $this->pageTitle($this->translate('Edit Watermark'), 1, $this->translate('Media Watermarking')); ?>

<div id="page-actions">
    <a href="<?php echo $this->url('admin/watermarker'); ?>" class="button"><?php echo $this->translate('Cancel'); ?></a>
    <button type="submit" form="edit-watermark-form" class="button"><?php echo $this->translate('Save'); ?></button>
</div>

<form id="edit-watermark-form" method="post" enctype="multipart/form-data">
    <!-- Basic Info Section -->
    <div class="section">
        <h3><?php echo $this->translate('Basic Settings'); ?></h3>
        
        <div class="field">
            <div class="field-meta">
                <label for="watermark-name"><?php echo $this->translate('Watermark Name'); ?></label>
                <div class="field-description"><?php echo $this->translate('Name for this watermark configuration.'); ?></div>
            </div>
            <div class="inputs">
                <input type="text" name="name" id="watermark-name" required 
                       value="<?php echo $this->escapeHtml($watermark['name'] ?? ''); ?>">
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="watermark-enabled"><?php echo $this->translate('Enabled'); ?></label>
                <div class="field-description"><?php echo $this->translate('Check to enable this watermark configuration.'); ?></div>
            </div>
            <div class="inputs">
                <input type="checkbox" name="enabled" id="watermark-enabled" value="1" 
                       <?php echo isset($watermark['enabled']) && $watermark['enabled'] ? 'checked' : ''; ?>>
            </div>
        </div>
        
        <input type="hidden" name="o:id" value="<?php echo $this->escapeHtml($watermark['id'] ?? ''); ?>">
    </div>

    <!-- Landscape Watermark -->
    <div class="section">
        <h3><?php echo $this->translate('Landscape Watermark'); ?> <span class="required">*</span></h3>
        <p class="watermarker-help"><?php echo $this->translate('Configure watermark for landscape-oriented images.'); ?></p>
        
        <div class="field">
            <div class="field-meta">
                <label for="landscape-image"><?php echo $this->translate('Watermark Image ID'); ?></label>
                <div class="field-description"><?php echo $this->translate('Enter the asset ID of your landscape watermark image.'); ?></div>
            </div>
            <div class="inputs">
                <input type="text" name="landscape_image" id="landscape-image" required
                       value="<?php echo $this->escapeHtml($landscape['media_id'] ?? ''); ?>">
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="landscape-position"><?php echo $this->translate('Position'); ?></label>
                <div class="field-description"><?php echo $this->translate('Select where the watermark should be positioned.'); ?></div>
            </div>
            <div class="inputs">
                <select name="landscape_position" id="landscape-position">
                    <?php 
                    $positions = [
                        'top-left' => $this->translate('Top Left'),
                        'top-right' => $this->translate('Top Right'),
                        'center' => $this->translate('Center'),
                        'bottom-left' => $this->translate('Bottom Left'),
                        'bottom-right' => $this->translate('Bottom Right'),
                        'bottom-full' => $this->translate('Bottom Full Width')
                    ];
                    $selected = $landscape['position'] ?? 'bottom-right';
                    foreach ($positions as $value => $label): 
                    ?>
                    <option value="<?php echo $value; ?>" 
                            <?php echo $value == $selected ? 'selected' : ''; ?>>
                        <?php echo $label; ?>
                    </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="landscape-opacity"><?php echo $this->translate('Opacity'); ?></label>
                <div class="field-description"><?php echo $this->translate('Set the opacity of the watermark (0.1 - 1.0).'); ?></div>
            </div>
            <div class="inputs">
                <input type="number" name="landscape_opacity" id="landscape-opacity" 
                       min="0.1" max="1.0" step="0.05" 
                       value="<?php echo $this->escapeHtml($landscape['opacity'] ?? 0.7); ?>">
            </div>
        </div>
    </div>
    
    <!-- Portrait Watermark -->
    <div class="section">
        <h3><?php echo $this->translate('Portrait Watermark'); ?> <span class="required">*</span></h3>
        <p class="watermarker-help"><?php echo $this->translate('Configure watermark for portrait-oriented images.'); ?></p>
        
        <div class="field">
            <div class="field-meta">
                <label for="portrait-image"><?php echo $this->translate('Watermark Image ID'); ?></label>
                <div class="field-description"><?php echo $this->translate('Enter the asset ID of your portrait watermark image.'); ?></div>
            </div>
            <div class="inputs">
                <input type="text" name="portrait_image" id="portrait-image" required
                       value="<?php echo $this->escapeHtml($portrait['media_id'] ?? ''); ?>">
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="portrait-position"><?php echo $this->translate('Position'); ?></label>
                <div class="field-description"><?php echo $this->translate('Select where the watermark should be positioned.'); ?></div>
            </div>
            <div class="inputs">
                <select name="portrait_position" id="portrait-position">
                    <?php 
                    $selected = $portrait['position'] ?? 'bottom-right';
                    foreach ($positions as $value => $label): 
                    ?>
                    <option value="<?php echo $value; ?>" 
                            <?php echo $value == $selected ? 'selected' : ''; ?>>
                        <?php echo $label; ?>
                    </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="portrait-opacity"><?php echo $this->translate('Opacity'); ?></label>
                <div class="field-description"><?php echo $this->translate('Set the opacity of the watermark (0.1 - 1.0).'); ?></div>
            </div>
            <div class="inputs">
                <input type="number" name="portrait_opacity" id="portrait-opacity" 
                       min="0.1" max="1.0" step="0.05" 
                       value="<?php echo $this->escapeHtml($portrait['opacity'] ?? 0.7); ?>">
            </div>
        </div>
    </div>
    
    <!-- Square Watermark -->
    <div class="section">
        <h3><?php echo $this->translate('Square Watermark (Optional)'); ?></h3>
        <p class="watermarker-help"><?php echo $this->translate('Configure watermark for square-shaped images. If not specified, landscape settings will be used.'); ?></p>
        
        <div class="field">
            <div class="field-meta">
                <label for="square-image"><?php echo $this->translate('Watermark Image ID'); ?></label>
                <div class="field-description"><?php echo $this->translate('Enter the asset ID of your square watermark image (optional).'); ?></div>
            </div>
            <div class="inputs">
                <input type="text" name="square_image" id="square-image"
                       value="<?php echo $this->escapeHtml($square['media_id'] ?? ''); ?>">
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="square-position"><?php echo $this->translate('Position'); ?></label>
                <div class="field-description"><?php echo $this->translate('Select where the watermark should be positioned.'); ?></div>
            </div>
            <div class="inputs">
                <select name="square_position" id="square-position">
                    <?php 
                    $selected = $square['position'] ?? 'bottom-right';
                    foreach ($positions as $value => $label): 
                    ?>
                    <option value="<?php echo $value; ?>" 
                            <?php echo $value == $selected ? 'selected' : ''; ?>>
                        <?php echo $label; ?>
                    </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
        
        <div class="field">
            <div class="field-meta">
                <label for="square-opacity"><?php echo $this->translate('Opacity'); ?></label>
                <div class="field-description"><?php echo $this->translate('Set the opacity of the watermark (0.1 - 1.0).'); ?></div>
            </div>
            <div class="inputs">
                <input type="number" name="square_opacity" id="square-opacity" 
                       min="0.1" max="1.0" step="0.05" 
                       value="<?php echo $this->escapeHtml($square['opacity'] ?? 0.7); ?>">
            </div>
        </div>
    </div>
    
    <div class="field">
        <div class="field-meta">
            <div class="field-description">
                <strong><?php echo $this->translate('How to find asset IDs:'); ?></strong> 
                <?php echo $this->translate('The asset ID is the number visible in the URL when viewing an asset in the admin interface (e.g., /admin/asset/12345 where 12345 is the ID).'); ?>
            </div>
        </div>
    </div>
    
    <!-- Simple hidden field for CSRF protection -->
    <input type="hidden" name="csrf" value="<?php echo md5(time()); ?>">
</form>