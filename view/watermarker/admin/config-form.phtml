<h2>Watermark Settings</h2>

<!-- Include CSRF Token Properly -->
<form method="post">
    <?php echo $this->formElement($this->form->get('csrf')); ?> <!-- Correct Omeka CSRF usage -->

    <div>
        <label for="watermark_portrait">Portrait Watermark:</label>
        <select name="watermark_portrait">
            <?php foreach ($this->assetOptions as $id => $name): ?>
                <option value="<?= $id ?>" <?= ($id == $this->watermarkPortrait) ? 'selected' : '' ?>>
                    <?= $name ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>

    <div>
        <label for="watermark_landscape">Landscape Watermark:</label>
        <select name="watermark_landscape">
            <?php foreach ($this->assetOptions as $id => $name): ?>
                <option value="<?= $id ?>" <?= ($id == $this->watermarkLandscape) ? 'selected' : '' ?>>
                    <?= $name ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>

    <div>
        <label for="enable_watermarking">Enable Watermarking:</label>
        <input type="checkbox" name="enable_watermarking" value="1" <?= $this->enableWatermarking ? 'checked' : '' ?>>
    </div>

    <div>
        <label for="reprocess_images">Reprocess Existing Images:</label>
        <button type="submit" name="reprocess_images" value="1">Reprocess Images</button>
    </div>

    <button type="submit">Save Changes</button>
</form>
